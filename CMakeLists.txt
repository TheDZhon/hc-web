#--> Cmake 2.6+ build script

CMAKE_MINIMUM_REQUIRED (VERSION 2.6 FATAL_ERROR)
PROJECT (hc001-web)

SET (TOP_DIR ${PROJECT_SOURCE_DIR})

SET (BIN_DIR ${TOP_DIR}/bin)
SET (SRC_DIR ${TOP_DIR}/src)

ADD_SUBDIRECTORY (src)
